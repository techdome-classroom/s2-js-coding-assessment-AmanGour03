/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function(s) {
    let stack=[];
    const b={
     ')':'(',
     '}':'{',
     ']':'[',
    };
    for(int i=0;i<s.length;i++)
};

module.exports = { isValid };


